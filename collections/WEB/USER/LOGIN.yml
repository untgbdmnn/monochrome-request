info:
  name: LOGIN
  type: http
  seq: 2

http:
  method: POST
  url: "{{HOST}}auth"
  body:
    type: json
    data: |-
      {
        "email": "monochrome@gmail.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
