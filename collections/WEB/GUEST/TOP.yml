info:
  name: TOP
  type: http
  seq: 3

http:
  method: POST
  url: "{{HOST}}guests/top"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
