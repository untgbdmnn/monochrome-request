info:
  name: UPDATE
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{HOST}}rewards/update/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
