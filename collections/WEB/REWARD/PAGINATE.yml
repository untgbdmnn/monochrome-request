info:
  name: PAGINATE
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL}}rewards/paginate"
  body:
    type: json
    data: |-
      {
        "page": 1,
        "limit": 5,
        "keyword": ""
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
