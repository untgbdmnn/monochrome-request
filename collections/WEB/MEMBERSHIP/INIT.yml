info:
  name: INIT
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL}}membership"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
