info:
  name: LIST
  type: http
  seq: 3

http:
  method: POST
  url: "{{HOST}}membership/list"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
